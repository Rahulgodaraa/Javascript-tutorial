/* IF ELSE STATEMENT */ 

let age = 30;
const votingage = 21;

if(age >= 21) {
    console.log(`Congratulations Now you can vote!`)
} else {
    yearsleft = votingage - age;
    console.log(`You are not adult yet,
    wait for ${yearsleft} to vote!`);
}

 